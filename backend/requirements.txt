# ---- Web framework ----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30

# ---- Veri doğrulama / ayarlar ----
pydantic>=2.7,<3.0
pydantic-settings>=2.3

# ---- Veritabanı ----
SQLAlchemy>=2.0,<2.1
GeoAlchemy2>=0.15          # SQLAlchemy'ye PostGIS "Geometry" tipini ekler
psycopg[binary]>=3.2       # PostgreSQL sürücüsü (v3)
alembic>=1.13              # şema migration'ları

# ---- Geometri işlemleri (Python tarafı) ----
shapely>=2.0               # geometri <-> GeoJSON dönüşümü

# ---- Kimlik doğrulama ----
PyJWT>=2.8                 # JWT üretme/doğrulama
bcrypt>=4.1                # parola özetleme (passlib yerine doğrudan)
python-multipart>=0.0.9    # Swagger'ın "Authorize" formu için
slowapi>=0.1.9             # istek hızı sınırı (kaba kuvvet koruması)

# ---- Yardımcılar ----
python-dotenv>=1.0
Faker>=25.0                # seed script: sahte varlık üretimi

# ---- Geliştirme / test ----
pytest>=8.0
httpx>=0.27                # FastAPI TestClient için gerekli
ruff>=0.5                  # lint + format
